# Local Development Environment Variables
# DO NOT commit this file to production
# This is for local testing only

# For local SQL Server development, you have options:

# Option 1: If using LocalDB (SQL Server Express LocalDB)
# DATABASE_URL="sqlserver://(localdb)\\mssqllocaldb;database=pryysm_dev;encrypt=true;trustServerCertificate=true;"

# Option 2: If using SQL Server on Docker
# DATABASE_URL="sqlserver://sa:YourPassword123@localhost:1433;database=pryysm_dev;encrypt=true;trustServerCertificate=true;"

# Option 3: For testing without database (in-memory)
# Use sqlite for development instead - just comment out above and uncomment below:
# DATABASE_URL="file:./dev.db"

# Option 4: If using Azure SQL for development (not recommended)
# DATABASE_URL="sqlserver://server.database.windows.net:1433;database=pryysm_dev;user=admin@server;password=YourPassword;encrypt=true;trustServerCertificate=false;connection timeout=30;"

# For now, using SQLite for local development
DATABASE_URL="file:./prisma/dev.db"

NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:3000/api
