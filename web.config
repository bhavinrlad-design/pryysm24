<?xml version="1.0" encoding="utf-8"?><?xml version="1.0" encoding="utf-8"?><?xml version="1.0" encoding="utf-8"?>

<configuration>

  <system.webServer><configuration><configuration>

    <webSocket enabled="false" />

    <handlers>  <system.webServer>  <system.webServer>

      <add name="iisnode" path="server.js" verb="*" modules="iisnode" />

    </handlers>    <handlers>    <webSocket enabled="false" />

    <rewrite>

      <rules>      <add name="iisnode" path="server.js" verb="*" modules="iisnode" />    <handlers>

        <rule name="DynamicContent">

          <conditions>    </handlers>      <add name="Node" path="*" verb="*" modules="httpPlatformHandler" />

            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="True" />

          </conditions>    <rewrite>    </handlers>

          <action type="Rewrite" url="server.js" />

        </rule>      <rules>    <httpPlatform stdoutLogEnabled="true" stdoutLogFile=".\node.log" startupTimeLimit="120" processPath="node" arguments="server.js" />

      </rules>

    </rewrite>        <rule name="NodeInspector" patternSyntax="ECMAScript" stopProcessing="true">    <httpErrors existingResponse="PassThrough" />

    <httpErrors existingResponse="PassThrough" />

  </system.webServer>          <match url="^server\.js\/debug[\/]?" />  </system.webServer>

</configuration>
        </rule></configuration>
        <rule name="StaticContent">
          <action type="Rewrite" url="public{REQUEST_URI}"/>
        </rule>
        <rule name="DynamicContent">
          <conditions>
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="True" />
          </conditions>
          <action type="Rewrite" url="server.js" />
        </rule>
      </rules>
    </rewrite>
    <security>
      <requestFiltering>
        <hiddenSegments>
          <add segment="node_modules" />
        </hiddenSegments>
      </requestFiltering>
    </security>
    <httpErrors existingResponse="PassThrough" />
  </system.webServer>
</configuration>