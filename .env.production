# Production environment variables for Azure deployment
# Replace these values with your actual Azure services information

# Database connection string (use Azure SQL Database or CosmosDB)
DATABASE_URL="postgresql://user:password@your-azure-postgres-server.postgres.database.azure.com:5432/pryysm?schema=public"

# Node environment
NODE_ENV=production

# Public API URL (adjust to your actual Azure app name)
NEXT_PUBLIC_API_URL=https://pryysm-app.azurewebsites.net/api

# Set this to control which port the app listens on (Azure uses 8080 by default)
PORT=8080

# Add other production environment variables as needed